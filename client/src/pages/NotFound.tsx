import { ArrowLeft, Compass, Home, SearchX } from "lucide-react";
import { useLocation } from "wouter";
import { Button } from "@/components/ui/button";
import { AppShell } from "@/components/layout/app-shell";

export default function NotFound() { const [, setLocation] = useLocation(); return <AppShell><div className="container flex min-h-[70vh] items-center justify-center py-16"><section className="relative w-full max-w-2xl overflow-hidden rounded-lg border border-border bg-card p-7 text-center surface-grid sm:p-12" aria-labelledby="not-found-title"><div className="pointer-events-none absolute -right-20 -top-20 size-52 rounded-full border border-[#dbe3d8]" /><span className="mx-auto flex size-14 items-center justify-center rounded-full bg-[#e8eef2] text-[#5c7890]"><SearchX className="size-6" /></span><p className="mt-6 text-[11px] font-bold uppercase tracking-[0.18em] text-muted-foreground">Record not found</p><h1 id="not-found-title" className="mt-2 font-display text-5xl tracking-[-0.04em]">This trail ends here.</h1><p className="mx-auto mt-4 max-w-md text-sm leading-7 text-muted-foreground">The workspace surface you requested does not exist or has moved. Return to the control center and continue from the verified record.</p><div className="mt-7 flex flex-wrap justify-center gap-3"><Button className="gap-2" onClick={() => setLocation("/dashboard")}><Home className="size-4" />Open dashboard</Button><Button variant="outline" className="gap-2" onClick={() => window.history.back()}><ArrowLeft className="size-4" />Go back</Button></div><div className="mt-8 flex items-center justify-center gap-2 text-[11px] text-muted-foreground"><Compass className="size-3.5" /> AI can recommend a path; you decide where to go next.</div></section></div></AppShell> }
