import { useEffect, useState } from "react";
import { Link } from "wouter";
import { ArrowUpRight, Bot, Check, Clock3, FileText, LockKeyhole, Sparkles, Users } from "lucide-react";
import { motion } from "framer-motion";
import { AppShell } from "@/components/layout/app-shell";
import { FlipText } from "@/components/shared/flip-text";
import { CaseStatusBadge } from "@/components/case/case-status-badge";
import { RiskIndicator } from "@/components/case/risk-indicator";
import { Button } from "@/components/ui/button";
import { dashboardData } from "@/mocks/workspace";
import { mockCaseService } from "@/services/case-service";
import type { CaseSummary } from "@/types/vindicai";

const reveal = { hidden: { opacity: 0, y: 12 }, show: { opacity: 1, y: 0, transition: { duration: 0.38, ease: "easeOut" as const } } };

export default function Dashboard() {
  const [cases, setCases] = useState<CaseSummary[]>([]);
  useEffect(() => { mockCaseService.listCases().then((result) => setCases(result.data)); }, []);
  return <AppShell><div className="container pb-16 pt-8 sm:pt-10"><motion.div initial="hidden" animate="show" variants={reveal} className="space-y-8"><section className="relative overflow-hidden rounded-lg border border-border bg-card px-5 py-7 surface-grid sm:px-8 sm:py-9"><div className="relative z-10 max-w-2xl"><div className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-[0.18em] text-[#65816c]"><Sparkles className="size-3.5" aria-hidden="true" /> Workspace control center</div><h1 className="mt-4 max-w-xl font-display text-[2.6rem] leading-[1.03] tracking-[-0.035em] text-foreground sm:text-5xl">Make the record <span className="text-[#76927b]">unmistakable.</span></h1><p className="mt-4 max-w-xl text-sm leading-7 text-muted-foreground sm:text-base">One calm surface for the facts, approvals, and next moves behind your highest-stakes disputes.</p><div className="mt-7 flex flex-wrap gap-3"><Button asChild className="gap-2 bg-primary px-4"><Link href="/cases/new">Start a case <ArrowUpRight className="size-4" /></Link></Button><Button asChild variant="outline" className="border-[#c8c2b7] bg-card/70"><Link href="/cases">Open case index</Link></Button></div></div><div className="pointer-events-none absolute -right-14 -top-20 hidden size-[290px] rounded-full border border-[#dbe3d8] sm:block" aria-hidden="true" /><div className="pointer-events-none absolute bottom-4 right-10 hidden w-36 text-right text-[10px] uppercase tracking-[0.16em] text-[#94a294] sm:block">AI recommends<br />human decides</div></section><section aria-labelledby="overview-heading"><div className="mb-3 flex items-end justify-between"><div><p className="text-[11px] font-bold uppercase tracking-[0.18em] text-muted-foreground">Workspace pulse</p><h2 id="overview-heading" className="mt-1 text-lg font-semibold">Your disputes, at a glance</h2></div><span className="text-xs text-muted-foreground">Updated moments ago</span></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{dashboardData.metrics.map((metric, index) => <motion.div key={metric.label} variants={reveal} transition={{ delay: index * .06 }} className="surface-lift rounded-md border border-border bg-card p-4 transition-transform"><div className="flex items-start justify-between"><span className="text-xs font-medium text-muted-foreground">{metric.label}</span><span className="text-[10px] font-semibold text-[#6f9478]">{metric.trend}</span></div><div className="mt-4 flex items-end justify-between"><FlipText text={metric.value} className="font-display text-3xl text-foreground" /><span className="text-[11px] text-muted-foreground">{metric.detail}</span></div></motion.div>)}</div></section><div className="grid items-start gap-5 xl:grid-cols-[minmax(0,1fr)_340px]"><section className="min-w-0 rounded-md border border-border bg-card" aria-labelledby="active-cases-heading"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><h2 id="active-cases-heading" className="text-base font-semibold">Active cases</h2><p className="mt-1 text-xs text-muted-foreground">The files currently asking for attention.</p></div><Button asChild variant="link" size="sm" className="gap-1"><Link href="/cases">View all <ArrowUpRight className="size-3" /></Link></Button></div><div className="divide-y divide-border/80">{cases.slice(0, 4).map((item) => <Link key={item.id} href={`/cases/${item.id}`} className="flex items-center justify-between gap-4 px-5 py-4 transition-colors hover:bg-[#faf9f5]"><div className="flex min-w-0 items-start gap-3"><span className="mt-0.5 flex size-8 shrink-0 items-center justify-center rounded-md bg-[#eef2ec] text-[#587762]"><FileText className="size-4" /></span><div className="min-w-0"><p className="truncate text-sm font-semibold">{item.title}</p><p className="mt-1 text-[11px] text-muted-foreground">{item.counterparty} · {item.amount}</p></div></div><div className="hidden shrink-0 items-center gap-3 sm:flex"><CaseStatusBadge status={item.status} /><RiskIndicator level={item.risk} /></div></Link>)}</div></section><div className="space-y-5"><section className="rounded-md border border-[#cfdcca] bg-[#f1f6ef] p-4" aria-labelledby="approval-heading"><div className="flex items-start justify-between"><div className="flex items-center gap-2"><span className="flex size-7 items-center justify-center rounded-md bg-[#dbe9d9] text-[#55755d]"><LockKeyhole className="size-4" /></span><div><p className="text-[10px] font-bold uppercase tracking-[0.15em] text-[#55755d]">Human control</p><h2 id="approval-heading" className="mt-0.5 text-sm font-semibold">Pending approvals</h2></div></div><span className="flex size-6 items-center justify-center rounded-full bg-[#f7ead9] text-[11px] font-bold text-[#976d38]">{dashboardData.approvals.length}</span></div><div className="mt-4 space-y-3">{dashboardData.approvals.map((item) => <Link key={item.id} href={`/cases/${item.caseId}`} className="flex gap-3"><Clock3 className="mt-0.5 size-4 text-[#a17a43]" /><div><p className="text-xs font-semibold">{item.title}</p><p className="mt-0.5 text-[11px] leading-relaxed text-muted-foreground">{item.counterparty} · {item.due}</p></div></Link>)}</div></section><section className="rounded-md border border-border bg-card p-4" aria-labelledby="activity-heading"><div className="flex items-center gap-2"><span className="flex size-7 items-center justify-center rounded-md bg-[#e8eef2] text-[#5c7890]"><Bot className="size-4" /></span><div><p className="text-[10px] font-bold uppercase tracking-[0.15em] text-muted-foreground">Intelligence activity</p><h2 id="activity-heading" className="mt-0.5 text-sm font-semibold">What changed recently</h2></div></div><div className="mt-4 space-y-4">{dashboardData.activity.map((item) => <div key={item.id} className="flex gap-3"><span className={`mt-1.5 size-1.5 shrink-0 rounded-full ${item.state === "verified" ? "bg-[#6f9478]" : item.state === "review" ? "bg-[#b58a52]" : "bg-[#6b8594]"}`} /><div><p className="text-xs font-semibold">{item.title}</p><p className="mt-0.5 text-[11px] leading-relaxed text-muted-foreground">{item.detail}</p><p className="mt-1 text-[10px] uppercase tracking-[0.12em] text-muted-foreground">{item.timestamp}</p></div></div>)}</div></section><div className="rounded-md border border-border bg-[#f5f1e8] p-4"><div className="flex items-start gap-3"><Users className="mt-0.5 size-4 text-[#8f7349]" /><p className="text-xs leading-relaxed text-muted-foreground">Every recommendation carries its source context. Review the record before approving a consequential action.</p></div></div></div></div></motion.div></div></AppShell>;
}
