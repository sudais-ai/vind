import { useState } from "react";
import { Check, ChevronRight, Database, Globe2, KeyRound, Palette, Save, Shield, Sparkles, UserRound, Bell, Boxes, LockKeyhole } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Switch } from "@/components/ui/switch";
import { Textarea } from "@/components/ui/textarea";
import { toast } from "sonner";
import { cn } from "@/lib/utils";

type Section = "profile" | "workspace" | "notifications" | "ai" | "integrations" | "security" | "privacy" | "data" | "appearance";
const sections: { id: Section; label: string; icon: typeof UserRound; description: string }[] = [
  { id: "profile", label: "Profile", icon: UserRound, description: "Personal identity" }, { id: "workspace", label: "Workspace", icon: Boxes, description: "Team context" }, { id: "notifications", label: "Notifications", icon: Bell, description: "Alert delivery" }, { id: "ai", label: "AI Preferences", icon: Sparkles, description: "Display preferences" }, { id: "integrations", label: "Integrations", icon: Globe2, description: "Connected tools" }, { id: "security", label: "Security", icon: Shield, description: "Access controls" }, { id: "privacy", label: "Privacy", icon: LockKeyhole, description: "Data choices" }, { id: "data", label: "Data", icon: Database, description: "Export and retention" }, { id: "appearance", label: "Appearance", icon: Palette, description: "Theme and density" },
];
function Field({ label, children, hint }: { label: string; children: React.ReactNode; hint?: string }) { return <label className="block"><span className="text-xs font-semibold">{label}</span>{children}{hint && <span className="mt-1 block text-[11px] text-muted-foreground">{hint}</span>}</label>; }
function SettingCard({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children: React.ReactNode }) { return <section className="rounded-lg border border-border bg-card p-5 shadow-sm sm:p-7"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-muted-foreground">{eyebrow}</p><h2 className="mt-2 text-lg font-semibold">{title}</h2><p className="mt-1 max-w-2xl text-sm leading-6 text-muted-foreground">{description}</p><div className="mt-6">{children}</div></section>; }
function ToggleRow({ label, description, checked, onChange }: { label: string; description: string; checked: boolean; onChange: (value: boolean) => void }) { return <div className="flex items-center justify-between gap-5 border-b border-border/70 py-4 last:border-0 last:pb-0 first:pt-0"><div><p className="text-sm font-semibold">{label}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{description}</p></div><Switch checked={checked} onCheckedChange={onChange} aria-label={label} /></div>; }
export default function Settings() {
  const [section, setSection] = useState<Section>(() => { const requested = new URLSearchParams(window.location.search).get("section") as Section | null; return sections.some((item) => item.id === requested) ? requested! : "profile"; }); const [saving, setSaving] = useState(false); const [saved, setSaved] = useState(false); const [profile, setProfile] = useState({ name: "Alex Morgan", email: "alex.morgan@northamerica.example", role: "Operations lead", timezone: "America/Los_Angeles", language: "English" }); const [workspace, setWorkspace] = useState({ name: "North America operations", description: "Source-aware dispute operations for the North America team." }); const [alerts, setAlerts] = useState({ email: true, inApp: true, approval: true, deadline: true, research: true, policy: false, security: true });
  const save = () => { if (!profile.name.trim() || !profile.email.includes("@")) { toast.error("Check your profile details", { description: "A name and valid email address are required." }); return; } setSaving(true); setSaved(false); window.setTimeout(() => { setSaving(false); setSaved(true); toast.success("Settings saved", { description: "Your mock workspace preferences were updated." }); }, 650); };
  const current = sections.find((item) => item.id === section)!;
  return <AppShell><div className="container max-w-6xl pb-16 pt-8 sm:pt-10"><div className="border-b border-border pb-7"><p className="text-[11px] font-bold uppercase tracking-[0.18em] text-muted-foreground">Step 19 · Workspace controls</p><h1 className="mt-2 font-display text-4xl tracking-[-0.035em]">Settings</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Keep personal preferences, workspace controls, and safety boundaries explicit. These settings never bypass human approval requirements.</p></div><div className="mt-7 grid gap-7 lg:grid-cols-[230px_minmax(0,1fr)]"><aside className="h-fit rounded-lg border border-border bg-card p-2" aria-label="Settings sections"><p className="px-3 py-2 text-[10px] font-bold uppercase tracking-[0.16em] text-muted-foreground">Configure</p>{sections.map((item) => { const Icon = item.icon; const active = section === item.id; return <button key={item.id} onClick={() => { setSection(item.id); setSaved(false); }} className={cn("flex w-full items-center gap-3 rounded-md px-3 py-2.5 text-left transition-colors", active ? "bg-primary text-primary-foreground" : "text-foreground hover:bg-accent")} aria-current={active ? "page" : undefined}><Icon className="size-4 shrink-0" /><span className="min-w-0 flex-1"><span className="block text-xs font-semibold">{item.label}</span><span className={cn("block text-[10px]", active ? "text-primary-foreground/70" : "text-muted-foreground")}>{item.description}</span></span><ChevronRight className="size-3.5 opacity-50" /></button>; })}</aside><main className="min-w-0"><div className="mb-4 flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-muted-foreground">Settings / {current.label}</p><h2 className="mt-1 text-xl font-semibold">{current.label}</h2></div>{["profile", "workspace", "notifications", "ai", "appearance"].includes(section) && <Button className="gap-2" onClick={save} disabled={saving}>{saved ? <Check className="size-4" /> : <Save className="size-4" />}{saving ? "Saving…" : saved ? "Saved" : "Save changes"}</Button>}</div>{section === "profile" && <SettingCard eyebrow="Personal identity" title="Profile details" description="This information appears on case ownership, approval, and audit records."><div className="grid gap-5 sm:grid-cols-2"><Field label="Profile image"><div className="mt-2 flex items-center gap-3"><span className="flex size-12 items-center justify-center rounded-full bg-primary text-sm font-bold text-primary-foreground">AM</span><Button variant="outline" size="sm">Change image</Button></div></Field><Field label="Full name"><Input className="mt-2" value={profile.name} onChange={(e) => setProfile({ ...profile, name: e.target.value })} /></Field><Field label="Email"><Input className="mt-2" type="email" value={profile.email} onChange={(e) => setProfile({ ...profile, email: e.target.value })} /></Field><Field label="Role"><Input className="mt-2" value={profile.role} onChange={(e) => setProfile({ ...profile, role: e.target.value })} /></Field><Field label="Timezone"><select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm" value={profile.timezone} onChange={(e) => setProfile({ ...profile, timezone: e.target.value })}><option>America/Los_Angeles</option><option>America/New_York</option><option>Europe/London</option></select></Field><Field label="Language"><select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm" value={profile.language} onChange={(e) => setProfile({ ...profile, language: e.target.value })}><option>English</option><option>Spanish</option><option>French</option></select></Field></div></SettingCard>}{section === "workspace" && <SettingCard eyebrow="Team context" title="Workspace profile" description="Workspace settings are shared context for your operations team, separate from your personal profile."><div className="space-y-5"><Field label="Workspace name"><Input className="mt-2" value={workspace.name} onChange={(e) => setWorkspace({ ...workspace, name: e.target.value })} /></Field><Field label="Description"><Textarea className="mt-2" value={workspace.description} onChange={(e) => setWorkspace({ ...workspace, description: e.target.value })} /></Field><div className="grid gap-4 rounded-md bg-muted/60 p-4 sm:grid-cols-2"><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-muted-foreground">Default workspace</p><p className="mt-1 text-sm font-semibold">North America ops</p></div><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-muted-foreground">Members</p><p className="mt-1 text-sm font-semibold">12 active reviewers</p></div></div></div></SettingCard>}{section === "notifications" && <SettingCard eyebrow="Alert delivery" title="Notification preferences" description="Choose where important workspace signals should arrive. Critical approval and security events remain visible in-app."><div>{Object.entries(alerts).map(([key, value]) => <ToggleRow key={key} label={({ email: "Email alerts", inApp: "In-app notifications", approval: "Approval required", deadline: "Deadline approaching", research: "Research and evidence", policy: "Policy changes", security: "Security alerts" } as Record<string, string>)[key] ?? key} description={({ email: "Receive a digest of selected workspace events.", inApp: "Show the bell badge and Notification Center updates.", approval: "Notify when a human decision is required.", deadline: "Surface approaching due dates and review windows.", research: "Notify when sources, citations, or research context change.", policy: "Notify when workspace policies are updated.", security: "Always notify on sign-in and access events." } as Record<string, string>)[key] ?? "Workspace alert preference."} checked={value} onChange={(checked) => setAlerts({ ...alerts, [key]: checked })} />)}</div></SettingCard>}{section === "ai" && <SettingCard eyebrow="Display preferences" title="AI preferences" description="These controls change how recommendations are displayed. They do not bypass safety, source verification, or human approval policies."><div className="grid gap-5 sm:grid-cols-2"><Field label="Analysis detail level"><select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>Standard</option><option>Detailed</option><option>Concise</option></select></Field><Field label="Research depth"><select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>Balanced</option><option>Deep review</option><option>Fast pass</option></select></Field><Field label="Confidence display"><select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>Show all signals</option><option>Show only warnings</option></select></Field><Field label="Citation visibility"><select className="mt-2 h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>Always visible</option><option>On demand</option></select></Field></div><div className="mt-6 rounded-md border border-[#cfdcca] bg-[#f1f6ef] p-4 text-xs leading-5 text-[#55755d]"><strong>Safety boundary:</strong> VindicAI recommendations remain source-aware and approval-gated regardless of these display preferences.</div></SettingCard>}{section === "integrations" && <SettingCard eyebrow="Connected tools" title="Integrations" description="Connect workspace tools when you are ready to move from mock records to live coordination."><div className="space-y-3"><Integration name="Google Workspace" detail="Calendar and Drive source context" status="Not connected" /><Integration name="Slack" detail="Approval and deadline notifications" status="Not connected" /><Integration name="Webhooks" detail="Event delivery for internal systems" status="Coming in Phase 2" /></div></SettingCard>}{section === "security" && <SettingCard eyebrow="Access controls" title="Security" description="Review access posture and keep consequential changes explicit."><div className="space-y-4"><InfoRow icon={KeyRound} label="Password" detail="Last changed 42 days ago" action="Change password" /><InfoRow icon={Shield} label="Two-factor authentication" detail="Recommended for approval owners" action="Set up" /><InfoRow icon={LockKeyhole} label="Active sessions" detail="2 approved workspace devices" action="Review" /></div></SettingCard>}{section === "privacy" && <SettingCard eyebrow="Data choices" title="Privacy" description="VindicAI keeps source context visible so reviewers can understand what informed a recommendation."><ToggleRow label="Product analytics" description="Share anonymous interaction data to improve workspace usability." checked={true} onChange={() => {}} /><ToggleRow label="Source preview retention" description="Keep mock source previews available in the case record." checked={true} onChange={() => {}} /></SettingCard>}{section === "data" && <SettingCard eyebrow="Records and retention" title="Data management" description="Export your workspace record or review retention defaults before connecting live sources."><div className="grid gap-3 sm:grid-cols-2"><Button variant="outline" className="h-auto justify-start p-4"><Database className="mr-3 size-4" /><span className="text-left"><span className="block text-sm font-semibold">Export workspace data</span><span className="mt-1 block text-xs text-muted-foreground">Download a reviewable JSON archive.</span></span></Button><Button variant="outline" className="h-auto justify-start p-4"><LockKeyhole className="mr-3 size-4" /><span className="text-left"><span className="block text-sm font-semibold">Retention policy</span><span className="mt-1 block text-xs text-muted-foreground">Review source and audit defaults.</span></span></Button></div></SettingCard>}{section === "appearance" && <SettingCard eyebrow="Theme and density" title="Appearance" description="VindicAI is intentionally Creamy White and light-first. Dark mode is reserved for a future fully themed pass so current pages stay consistent."><Field label="Theme"><select className="mt-2 h-10 w-full max-w-sm rounded-md border border-input bg-background px-3 text-sm" defaultValue="light"><option value="light">Light · Creamy White (current)</option><option value="system">System · Phase 2 preview</option><option value="dark">Dark · Phase 2 completion item</option></select></Field><div className="mt-6 rounded-md border border-border bg-muted/50 p-4 text-xs leading-5 text-muted-foreground">Dark mode controls are intentionally not activated yet; enabling a partial theme would break the existing Phase 1 visual system.</div></SettingCard>}</main></div></div></AppShell>;
}
function Integration({ name, detail, status }: { name: string; detail: string; status: string }) { return <div className="flex flex-col gap-3 rounded-md border border-border bg-background p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold">{name}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></div><Button variant="outline" size="sm">{status === "Not connected" ? "Connect" : status}</Button></div>; }
function InfoRow({ icon: Icon, label, detail, action }: { icon: typeof KeyRound; label: string; detail: string; action: string }) { return <div className="flex items-center gap-3 rounded-md border border-border bg-background p-4"><Icon className="size-4 text-muted-foreground" /><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{label}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></div><Button variant="outline" size="sm">{action}</Button></div>; }
