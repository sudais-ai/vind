import { useEffect, useState } from "react";
import { ArrowRight, BookOpen, CheckCircle2, FileText, LockKeyhole, ShieldAlert } from "lucide-react";
import { Link, useRoute } from "wouter";
import { AppShell } from "@/components/layout/app-shell";
import { Button } from "@/components/ui/button";
import { mockIntelligenceService, type StrategyRecord } from "@/services/intelligence-service";

export default function CaseStrategy() {
  const [, params] = useRoute("/cases/:id/strategy");
  const [strategy, setStrategy] = useState<StrategyRecord>();
  useEffect(() => { if (params?.id) mockIntelligenceService.getStrategy(params.id).then(({ data }) => setStrategy(data)); }, [params?.id]);
  if (!strategy) return <AppShell><div className="container py-20 text-center text-sm text-muted-foreground">Loading strategy workspace...</div></AppShell>;
  return <AppShell><div className="container pb-16 pt-8 sm:pt-10"><Link href={`/cases/${strategy.caseId}`} className="text-xs text-muted-foreground hover:text-foreground">← Case Room</Link><div className="mt-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[11px] font-bold uppercase tracking-[0.18em] text-muted-foreground">Step 13 · Strategy layer</p><h1 className="mt-2 font-display text-4xl tracking-[-0.035em]">{strategy.title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">A proposed path that keeps source context, risk, and human approval visible before a draft or action moves forward.</p></div><span className={`flex w-fit items-center gap-2 rounded-md px-3 py-2 text-xs font-semibold ${strategy.risk === "critical" || strategy.risk === "high" ? "bg-[#f8e7e2] text-[#a4584c]" : "bg-[#f7ead9] text-[#976d38]"}`}><ShieldAlert className="size-4" /> {strategy.risk} risk</span></div><section className="mt-8 grid gap-5 lg:grid-cols-[minmax(0,1fr)_340px]"><div className="space-y-5"><section className="rounded-md border border-[#cfdcca] bg-[#f1f6ef] p-5 sm:p-6"><p className="text-[10px] font-bold uppercase tracking-[0.15em] text-[#55755d]">AI-proposed strategy</p><h2 className="mt-2 text-xl font-semibold">Build a reviewable next move.</h2><p className="mt-3 text-sm leading-7 text-foreground">{strategy.summary}</p></section><section className="rounded-md border border-border bg-card p-5 sm:p-6"><div className="flex items-center gap-2"><CheckCircle2 className="size-4 text-[#55755d]" /><h2 className="text-sm font-semibold">Strategy steps</h2></div><div className="mt-5 space-y-4">{strategy.steps.map((step, index) => <div key={step} className="flex gap-3"><span className="flex size-6 shrink-0 items-center justify-center rounded-full bg-[#e4eee5] text-[10px] font-bold text-[#55755d]">{index + 1}</span><p className="text-sm leading-6">{step}</p></div>)}</div></section></div><aside className="space-y-5"><section className="rounded-md border border-border bg-card p-5"><div className="flex items-center gap-2"><BookOpen className="size-4 text-[#5c7890]" /><h2 className="text-sm font-semibold">Supporting citations</h2></div><div className="mt-4 space-y-3">{strategy.citations.map((citation) => <div key={citation} className="rounded-md bg-[#f7f5ef] p-3 text-xs leading-5">{citation}</div>)}</div></section><section className="rounded-md border border-[#ead9ba] bg-[#fbf5e9] p-5"><div className="flex items-center gap-2"><LockKeyhole className="size-4 text-[#a17a43]" /><h2 className="text-sm font-semibold">Human sign-off required</h2></div><p className="mt-2 text-xs leading-5 text-muted-foreground">The strategy can inform a draft, but it cannot approve or execute an action by itself.</p><div className="mt-4 flex flex-col gap-2"><Button asChild className="gap-2 bg-primary"><Link href={strategy.approvalId ? `/cases/${strategy.caseId}/approvals` : "/approvals"}>Review approval <ArrowRight className="size-3.5" /></Link></Button>{strategy.draftId && <Button asChild variant="outline" className="gap-2"><Link href={`/cases/${strategy.caseId}/drafts`}><FileText className="size-3.5" /> Open draft</Link></Button>}</div></section></aside></section></div></AppShell>;
}
