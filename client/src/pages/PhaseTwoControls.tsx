import { useState } from "react";
import { ArrowRight, Check, ChevronRight, Download, ExternalLink, FileKey2, LockKeyhole, Mail, ShieldCheck, Trash2, UsersRound, X } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { toast } from "@/components/ui/sonner";
import { cn } from "@/lib/utils";
import { accessHistory, analytics, approvalPolicies, dataOverview, gmailIntegration, members, permissionRules, teams } from "@/mocks/phase-two";
import type { ConnectionState, ConsentStage, ExportState, WorkspaceRole } from "@/types/phase-two";

type Section = "integrations" | "privacy" | "workspace";
const roleTone: Record<WorkspaceRole,string> = { OWNER:"bg-[#e3dfd5] text-[#18324b]", ADMIN:"bg-[#e8eef2] text-[#5c7890]", MANAGER:"bg-[#e4eee5] text-[#55755d]", REVIEWER:"bg-[#fbf5e9] text-[#976d38]", MEMBER:"bg-muted text-muted-foreground", VIEWER:"bg-[#f0ede6] text-[#667169]" };
function SectionNav({ section, setSection }: { section: Section; setSection: (value:Section)=>void }) { const items: [Section,string,string][] = [["integrations","Integrations","Least-privilege connections"],["privacy","Privacy & data","Export, retention, access"],["workspace","Workspace admin","People, policy, analytics"]]; return <aside className="h-fit rounded-lg border border-border bg-card p-2"><p className="px-3 py-2 text-[10px] font-bold uppercase tracking-[0.16em] text-muted-foreground">Control center</p>{items.map(([id,label,detail])=><button key={id} onClick={()=>setSection(id)} className={cn("flex w-full items-center gap-3 rounded-md px-3 py-3 text-left",section===id?"bg-primary text-primary-foreground":"hover:bg-accent")}><span className="min-w-0 flex-1"><span className="block text-xs font-semibold">{label}</span><span className={cn("mt-0.5 block text-[10px]",section===id?"text-primary-foreground/70":"text-muted-foreground")}>{detail}</span></span><ChevronRight className="size-3.5 opacity-60" /></button>)}</aside> }
function Header({ section }: { section: Section }) { const meta = { integrations:["Step 20 · Consent center","Gmail permissions"], privacy:["Step 21 · Data controls","Privacy & data"], workspace:["Step 22 · Enterprise workspace","Workspace administration"] }[section]; return <div className="border-b border-border pb-7"><p className="text-[11px] font-bold uppercase tracking-[0.18em] text-muted-foreground">{meta[0]}</p><h1 className="mt-2 font-display text-4xl tracking-[-0.035em]">{meta[1]}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">{section === "integrations" ? "Connect selected dispute communications without granting mailbox-wide access." : section === "privacy" ? "Make records, retention examples, and data-access history visible before connecting live sources." : "Keep people, roles, approvals, and operational outcomes explicit across the workspace."}</p></div> }
function GmailConsent() { const [state,setState]=useState<ConnectionState>(gmailIntegration.state); const [stage,setStage]=useState<ConsentStage>("Connect Gmail"); const stages: ConsentStage[]=["Connect Gmail","Review Access","Understand Data Usage","Confirm","Connected"]; const advance=()=>{ if(stage === "Connect Gmail"){setState("CONNECTING");window.setTimeout(()=>{setState("PERMISSION_REQUIRED");setStage("Review Access")},450)} else if(stage === "Review Access") setStage("Understand Data Usage"); else if(stage === "Understand Data Usage") setStage("Confirm"); else if(stage === "Confirm"){setState("CONNECTED");setStage("Connected")} }; return <div className="space-y-5"><section className="rounded-lg border border-[#cfdcca] bg-[#f1f6ef] p-5 sm:p-7"><div className="flex items-start gap-3"><span className="flex size-9 items-center justify-center rounded-md bg-[#dbe9d9] text-[#55755d]"><ShieldCheck className="size-5" /></span><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#55755d]">Least privilege by default</p><h2 className="mt-1 text-lg font-semibold">Your inbox stays yours.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">VindicAI would only access selected relevant threads and attachments that you intentionally add to a dispute record. It does not request access to your entire inbox.</p></div></div></section><section className="rounded-lg border border-border bg-card p-5 sm:p-7"><div className="flex flex-col gap-4 border-b border-border pb-6 sm:flex-row sm:items-start sm:justify-between"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-md bg-[#f8e7e2] text-[#a84e43]"><Mail className="size-5" /></span><div><h2 className="font-semibold">{gmailIntegration.name}</h2><p className="mt-1 text-xs text-muted-foreground">{state === "CONNECTED" ? "Connected to selected dispute context" : state === "CONNECTING" ? "Opening secure review…" : state === "PERMISSION_REQUIRED" ? "Review permission scope" : "Not connected"}</p></div></div><span className={cn("rounded-full px-2 py-1 text-[10px] font-bold uppercase tracking-[0.08em]",state === "CONNECTED"?"bg-[#e4eee5] text-[#55755d]":state === "ERROR"?"bg-[#f8e7e2] text-[#a84e43]":"bg-[#fbf5e9] text-[#976d38]")}>{state.replaceAll("_"," ")}</span></div><div className="mt-6 grid gap-2 sm:grid-cols-5">{stages.map((item,index)=><div key={item} className={cn("rounded-md border p-3",stages.indexOf(stage)>=index?"border-[#b8cdb8] bg-[#f1f6ef]":"border-border bg-background")}><div className="flex items-center gap-2"><span className={cn("flex size-5 items-center justify-center rounded-full text-[10px]",stages.indexOf(stage)>=index?"bg-[#7c9c82] text-white":"bg-muted text-muted-foreground")}>{stages.indexOf(stage)>index?<Check className="size-3"/>:index+1}</span><span className="text-[11px] font-semibold">{item}</span></div></div>)}</div>{stage !== "Connected" ? <div className="mt-7 space-y-5"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground">Permission summary</p><div className="mt-3 divide-y divide-border rounded-md border border-border">{gmailIntegration.permissions.map(item=><div key={item.label} className="flex items-start gap-3 p-4"><span className={cn("mt-0.5 flex size-5 items-center justify-center rounded-full",item.enabled?"bg-[#e4eee5] text-[#55755d]":"bg-muted text-muted-foreground")}>{item.enabled?<Check className="size-3"/>:<X className="size-3"/>}</span><div><p className="text-sm font-semibold">{item.label}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{item.detail}</p></div></div>)}</div></div><div className="flex flex-wrap justify-end gap-2"><Button variant="outline" onClick={()=>{setStage("Connect Gmail");setState("NOT_CONNECTED")}}>Start over</Button><Button onClick={advance} disabled={state === "CONNECTING"}>{stage === "Connect Gmail"?"Connect Gmail":stage === "Confirm"?"Confirm selected access":"Continue"}<ArrowRight className="ml-2 size-4"/></Button></div></div> : <div className="mt-7 grid gap-4 sm:grid-cols-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-muted-foreground">Account</p><p className="mt-1 text-sm font-semibold">alex.morgan@northamerica.example</p></div><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-muted-foreground">Last sync</p><p className="mt-1 text-sm font-semibold">Just now · selected threads</p></div><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-muted-foreground">Connected</p><p className="mt-1 text-sm font-semibold">September 11, 2026</p></div><div className="sm:col-span-3 rounded-md border border-[#cfdcca] bg-[#f1f6ef] p-4 text-xs leading-5 text-[#55755d]"><strong>Human control:</strong> VindicAI can organize and analyze imported records, but it cannot send, delete, or act on your behalf without explicit permission and approval.</div></div>}</section><section className="rounded-md border border-border bg-card p-5"><div className="flex items-center gap-3"><LockKeyhole className="size-4 text-[#8f7349]"/><p className="text-xs leading-5 text-muted-foreground">This is a frontend-only mock consent flow. No OAuth token, mailbox data, or email content is accessed or stored.</p></div></section></div> }
function Privacy() { const [exportState,setExportState]=useState<ExportState>("Idle"); const [selected,setSelected]=useState(dataOverview.map(item=>item.label)); const [deleteStep,setDeleteStep]=useState(0); const toggle=(label:string)=>setSelected(current=>current.includes(label)?current.filter(item=>item!==label):[...current,label]); const exportFlow=()=>{setExportState("Preparing");window.setTimeout(()=>setExportState("Ready"),650)}; return <div className="space-y-5"><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{dataOverview.map(item=><button key={item.label} onClick={()=>toggle(item.label)} className={cn("rounded-md border p-4 text-left transition-colors",selected.includes(item.label)?"border-[#b8cdb8] bg-[#f7faf5]":"border-border bg-card opacity-60")}><div className="flex items-start justify-between gap-3"><span className="text-sm font-semibold">{item.label}</span><span className="text-xs font-bold text-[#55755d]">{item.count}</span></div><p className="mt-2 text-xs leading-5 text-muted-foreground">{item.detail}</p></button>)}</section><section className="rounded-lg border border-border bg-card p-5 sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground">Data export</p><h2 className="mt-1 text-lg font-semibold">A reviewable workspace record</h2><p className="mt-1 text-sm text-muted-foreground">Select data, review scope, then prepare a mock export. No file is generated in this preview.</p></div><Download className="size-5 text-[#5c7890]"/></div><div className="mt-5 flex flex-wrap items-center gap-2 text-xs"><span className="rounded-full bg-secondary px-3 py-1.5">{selected.length} sections selected</span><span className="rounded-full bg-secondary px-3 py-1.5">Mock only</span><Button size="sm" className="ml-auto" onClick={exportFlow} disabled={exportState === "Preparing"}>{exportState === "Idle"?"Review export":exportState === "Preparing"?"Preparing…":exportState === "Ready"?"Ready to download":"Retry export"}</Button></div>{exportState === "Ready" && <div className="mt-4 rounded-md border border-[#b8cdb8] bg-[#f1f6ef] p-4 text-xs text-[#55755d]"><strong>Export ready.</strong> A future backend would provide the file here after access checks.</div>}</section><section className="grid gap-5 lg:grid-cols-[1fr_1fr]"><div className="rounded-lg border border-border bg-card p-5"><div className="flex items-center gap-2"><FileKey2 className="size-4 text-[#8f7349]"/><h2 className="text-sm font-semibold">Illustrative retention policy</h2></div><p className="mt-2 text-xs leading-5 text-muted-foreground">Configurable examples for a future backend, not legal guarantees.</p><div className="mt-4 space-y-3 text-xs"><div className="flex justify-between border-b border-border pb-3"><span>Evidence</span><strong>180 days</strong></div><div className="flex justify-between border-b border-border pb-3"><span>Research</span><strong>365 days</strong></div><div className="flex justify-between"><span>Activity logs</span><strong>1 year</strong></div></div></div><div className="rounded-lg border border-[#ead1cb] bg-[#fdf5f2] p-5"><div className="flex items-center gap-2 text-[#a84e43]"><Trash2 className="size-4"/><h2 className="text-sm font-semibold">Delete workspace data</h2></div><p className="mt-2 text-xs leading-5 text-muted-foreground">This mock flow represents deletion of selected workspace records. Retention obligations or security logs may remain under the configured policy.</p>{deleteStep===0?<Button variant="outline" className="mt-4 border-[#d4aaa2] text-[#8f3d35]" onClick={()=>setDeleteStep(1)}>Review deletion scope</Button>:deleteStep===1?<div className="mt-4 flex gap-2"><Button variant="outline" onClick={()=>setDeleteStep(0)}>Cancel</Button><Button className="bg-[#8f3d35] text-white" onClick={()=>{setDeleteStep(2);toast.success("Mock deletion completed",{description:"No real data was deleted."})}}>Confirm mock deletion</Button></div>:<p className="mt-4 text-xs font-semibold text-[#8f3d35]">Mock completion recorded. No real data was deleted.</p>}</div></section><section className="rounded-lg border border-border bg-card p-5"><div className="flex items-center gap-2"><LockKeyhole className="size-4 text-[#5c7890]"/><h2 className="text-sm font-semibold">Data access history</h2></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[640px] text-left text-xs"><thead className="border-b border-border text-[10px] uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="pb-3">Who</th><th className="pb-3">What</th><th className="pb-3">When</th><th className="pb-3">Why</th></tr></thead><tbody className="divide-y divide-border/80">{accessHistory.map(item=><tr key={item.id}><td className="py-3 font-semibold">{item.actor}</td><td className="py-3">{item.action} · {item.resource}</td><td className="py-3 text-muted-foreground">{item.timestamp}</td><td className="py-3 text-muted-foreground">{item.reason}</td></tr>)}</tbody></table></div></section></div> }
function Workspace() { const [roleFilter,setRoleFilter]=useState<WorkspaceRole|"ALL">("ALL"); const [memberRows,setMemberRows]=useState(members); const changeRole=(id:string)=>setMemberRows(rows=>rows.map(row=>row.id===id?{...row,role:row.role==="VIEWER"?"REVIEWER":"VIEWER"}:row)); return <div className="space-y-5"><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[{label:"Organization",value:"North America operations"},{label:"Members",value:"12 active · 1 invited"},{label:"Active cases",value:"08 across 3 teams"},{label:"Integrations",value:"1 configured · Gmail"}].map(item=><div key={item.label} className="rounded-md border border-border bg-card p-4"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground">{item.label}</p><p className="mt-3 text-sm font-semibold">{item.value}</p></div>)}</section><section className="rounded-lg border border-border bg-card p-5 sm:p-7"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground">Team management</p><h2 className="mt-1 text-lg font-semibold">Teams and ownership</h2></div><Button size="sm" onClick={()=>toast.info("Mock team creation",{description:"A future backend would create the team after admin validation."})}><UsersRound className="mr-2 size-4"/> Add team</Button></div><div className="mt-5 grid gap-3 md:grid-cols-3">{teams.map(team=><div key={team.id} className="rounded-md border border-border bg-background p-4"><p className="text-sm font-semibold">{team.name}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{team.detail}</p><div className="mt-4 flex justify-between text-[11px]"><span>{team.members} members</span><span className="font-semibold">Lead · {team.lead}</span></div></div>)}</div></section><section className="rounded-lg border border-border bg-card p-5 sm:p-7"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground">Member management</p><h2 className="mt-1 text-lg font-semibold">People and roles</h2></div><select value={roleFilter} onChange={e=>setRoleFilter(e.target.value as WorkspaceRole|"ALL")} className="h-9 rounded-md border border-input bg-background px-2 text-xs"><option value="ALL">All roles</option>{Object.keys(roleTone).map(role=><option key={role}>{role}</option>)}</select></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[720px] text-left text-xs"><thead className="border-b border-border text-[10px] uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="pb-3">Member</th><th className="pb-3">Team</th><th className="pb-3">Role</th><th className="pb-3">Status</th><th className="pb-3">Joined</th><th className="pb-3">Actions</th></tr></thead><tbody className="divide-y divide-border/80">{memberRows.filter(row=>roleFilter==="ALL"||row.role===roleFilter).map(row=><tr key={row.id}><td className="py-3"><p className="font-semibold">{row.name}</p><p className="mt-1 text-muted-foreground">{row.email}</p></td><td className="py-3">{row.team}</td><td className="py-3"><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${roleTone[row.role]}`}>{row.role}</span></td><td className="py-3">{row.status}</td><td className="py-3 text-muted-foreground">{row.joined}</td><td className="py-3"><Button variant="ghost" size="sm" onClick={()=>changeRole(row.id)}>Change role</Button></td></tr>)}</tbody></table></div></section><section className="rounded-lg border border-border bg-card p-5 sm:p-7"><div className="flex items-center gap-2"><ShieldCheck className="size-4 text-[#55755d]"/><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground">RBAC and approval policy</p><h2 className="mt-1 text-lg font-semibold">Who can see, edit, approve, and execute</h2></div></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="border-b border-border text-[10px] uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="pb-3">Permission group</th><th className="pb-3">See</th><th className="pb-3">Edit</th><th className="pb-3">Approve</th><th className="pb-3">Execute</th></tr></thead><tbody className="divide-y divide-border/80">{permissionRules.map(rule=><tr key={rule.group}><td className="py-3 font-semibold">{rule.group}</td><td className="py-3">{rule.see}</td><td className="py-3">{rule.edit}</td><td className="py-3">{rule.approve}</td><td className="py-3">{rule.execute}</td></tr>)}</tbody></table></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{approvalPolicies.map(policy=><div key={policy.tier} className="rounded-md border border-border bg-background p-4"><p className="text-xs font-bold">{policy.tier}</p><p className="mt-2 text-xs leading-5 text-muted-foreground">{policy.rule}</p><p className="mt-3 text-[10px] font-bold uppercase tracking-[0.1em] text-[#55755d]">{policy.approvers}</p></div>)}</div><p className="mt-4 text-[11px] leading-5 text-muted-foreground">Illustrative policy UI only. A future backend will enforce authorization and approval rules deterministically; this preview does not grant permissions.</p></section><section className="rounded-lg border border-border bg-card p-5 sm:p-7"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-muted-foreground">Enterprise analytics</p><h2 className="mt-1 text-lg font-semibold">Operational signal, not decoration</h2></div><ExternalLink className="size-4 text-muted-foreground"/></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{analytics.map(item=><div key={item.label} className="rounded-md border border-border bg-background p-4"><p className="text-xs text-muted-foreground">{item.label}</p><p className="mt-3 font-display text-3xl">{item.value}</p><p className="mt-1 text-[11px] text-muted-foreground">{item.detail}</p><p className="mt-3 text-[10px] font-bold uppercase tracking-[0.1em] text-[#55755d]">{item.change}</p></div>)}</div><div className="mt-5 flex items-center justify-between rounded-md bg-[#f5f1e8] p-4"><div><p className="text-xs font-semibold">Audit export</p><p className="mt-1 text-xs text-muted-foreground">Preview {accessHistory.length} events with filters before a future export.</p></div><Button variant="outline" size="sm" onClick={()=>toast.info("Mock audit export",{description:"Export selection saved for a future backend."})}>Select events <Download className="ml-2 size-3.5"/></Button></div></section></div> }
export default function PhaseTwoControls({ initialSection="integrations" }: { initialSection?: Section }) { const [section,setSection]=useState<Section>(initialSection); return <AppShell><div className="container max-w-7xl pb-16 pt-8 sm:pt-10"><Header section={section}/><div className="mt-7 grid gap-7 lg:grid-cols-[230px_minmax(0,1fr)]"><SectionNav section={section} setSection={setSection}/><main className="min-w-0">{section === "integrations"?<GmailConsent/>:section === "privacy"?<Privacy/>:<Workspace/>}</main></div></div></AppShell> }
