import { useState } from "react"; export function Switch({ checked, onCheckedChange, 'aria-label': ariaLabel }: { checked?: boolean; onCheckedChange?: (value:boolean)=>void; 'aria-label'?: string }) { const [local,setLocal]=useState(Boolean(checked)); const value=checked ?? local; return <button type="button" role="switch" aria-checked={value} aria-label={ariaLabel} onClick={()=>{const next=!value;setLocal(next);onCheckedChange?.(next)}} className={`relative h-6 w-10 rounded-full ${value?"bg-primary":"bg-muted"}`}><span className={`absolute top-1 size-4 rounded-full bg-card transition-transform ${value?"left-5":"left-1"}`} /></button>; }
