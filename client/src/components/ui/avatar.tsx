import type { HTMLAttributes, ImgHTMLAttributes } from 'react'; export function Avatar({className,children}:{className?:string;children?:React.ReactNode}){return <span className={'inline-flex size-9 items-center justify-center overflow-hidden rounded-full bg-muted '+(className??'')}>{children}</span>}; export function AvatarImage(p:ImgHTMLAttributes<HTMLImageElement>){return <img {...p} className="size-full object-cover"/>}; export function AvatarFallback({className,children,...props}:HTMLAttributes<HTMLSpanElement>){return <span {...props} className={'text-xs font-semibold '+(className??'')}>{children}</span>}
