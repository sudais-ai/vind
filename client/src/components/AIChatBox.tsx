export type Message={id:string;role:'user'|'assistant';content:string}; export function AIChatBox({messages=[]}:{messages?:Message[];onSendMessage?:(content:string)=>void;isLoading?:boolean;placeholder?:string;height?:string;emptyStateMessage?:string;suggestedPrompts?:string[]}){return <div className="rounded-md border border-border bg-card p-4 text-sm text-muted-foreground">{messages.length ? `${messages.length} messages in preview` : 'AI workspace preview'}</div>}
